// utilities helpful for several part of the project
#include "gpuarray.h"

namespace utils
{

	// checks whether two FloatArrays are of same shape
	bool check_shape_eq(const FloatArray&, const FloatArray&);
	
}